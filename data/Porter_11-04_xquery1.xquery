declare default element namespace "http://www.tei-c.org/ns/1.0";
collection("file:///Users/isabelporter/Desktop/Iliad/data")
(: Couldn't get this to work all of recitation, I was planning to do something like for $head in distrinct-values(//head) return $head for the first bullet on the list, then use XML with {} for the next couple of bullet points to get an alpabetized list :)